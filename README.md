# IdeaSpark

A tiny web app that generates fresh app ideas with one click.

## Run locally

Use any static server. For example:

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000>.
